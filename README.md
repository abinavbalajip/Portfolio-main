# Abinav — Portfolio

This repository contains a modern, responsive portfolio built with Next.js and Tailwind CSS.

Getting started

1. Install dependencies:

```powershell
npm install
```

2. Run the dev server:

```powershell
npm run dev
```

Notes

- Update `app/components/Contact.jsx` with your real email and LinkedIn.
- Add screenshots to `public/` and reference them in the projects list.
